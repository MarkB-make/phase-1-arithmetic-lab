// Define the numbers for multiplication that result in 62
const num1 = 31;
const num2 = 2;
const multiply = num1 * num2; // Should equal 62

// Generate a random integer greater than 0
const random = Math.floor(Math.random() * 10) + 1;

// Define numbers for modulo operation that results in remainder of 4
const num3 = 22;
const num4 = 6;
const mod = num3 % num4; // Should equal 4

// Find the maximum value in a set that returns 20
const max = Math.max(1, 5, 10, 20, 15);
